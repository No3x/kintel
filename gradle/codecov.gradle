defaultTasks 'check'

apply plugin: 'java'

/* generate Java code coverage reports */
apply plugin: 'jacoco'
codeCoverageReport {
    reports {
        xml.enabled true
        html.enabled true
    }
}

repositories {
    jcenter()
}
